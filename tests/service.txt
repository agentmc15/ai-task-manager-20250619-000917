Have everything I need. Quick observation from image 5: the Project class uses PostgreSQL's `JSONB` via `JSONB as JSONNB`. The enriched column should use the same for consistency (not `JSON`).

Let's start building.

## Edit 1 — `db/manager.py`: Add `_ensure_enriched_columns`

### Edit 1a: Wire into `init_sql_tables`

**FIND:**
```
    # Ensure submission lifecycle columns exist on project table
    _ensure_status_columns(eng)
```

**REPLACE WITH:**
```
    # Ensure submission lifecycle columns exist on project table
    _ensure_status_columns(eng)

    # Ensure enrichment columns exist on project table
    _ensure_enriched_columns(eng)
```

### Edit 1b: Add the function after `_ensure_status_columns`

**FIND** the end of `_ensure_status_columns` — the last lines should be:
```
            conn.commit()
            log.info("idx_project_status index created")
```

**REPLACE WITH:**
```
            conn.commit()
            log.info("idx_project_status index created")


def _ensure_enriched_columns(eng):
    """Add enriched and enriched_json columns to project table if they don't exist.
    These are populated by the inline enrichment step at submit time. The
    enriched_json column holds the full 21-question payload consumed by the
    downstream Archer publisher service.
    """
    with eng.connect() as conn:
        # enriched
        result = conn.execute(text("""
            SELECT column_name FROM information_schema.columns
            WHERE table_name = 'project' AND column_name = 'enriched'
        """))
        if not result.fetchone():
            log.info("Adding enriched column to project table...")
            conn.execute(text(
                "ALTER TABLE project ADD COLUMN enriched BOOLEAN NOT NULL DEFAULT false"
            ))
            conn.commit()
            log.info("enriched column added (default false for existing rows)")

        # enriched_json
        result = conn.execute(text("""
            SELECT column_name FROM information_schema.columns
            WHERE table_name = 'project' AND column_name = 'enriched_json'
        """))
        if not result.fetchone():
            log.info("Adding enriched_json column to project table...")
            conn.execute(text(
                "ALTER TABLE project ADD COLUMN enriched_json JSONB NULL"
            ))
            conn.commit()
            log.info("enriched_json column added")
```

Save. Don't rebuild yet — we'll batch after Edit 2.

## Edit 2 — `models/questionnaire.py`: Add fields to `Project`

Based on image 5, the field order I can read is: `id, title, description, tags, user_id, owner_email, questionnaire_id, questionnaire, created, updated, responses_json, status, submitted_at, attributes, archer_submitted_at, archer_content_id`.

Send a screenshot of the `Project` class from line 183 (`class Project(SQLModel, table=True):`) down through line ~210 so I can see the exact line where `archer_submitted_at` is declared — I want to put the new fields right before `archer_submitted_at` so enrichment fields group naturally with status/submitted_at (which we added in the last session).

Once I have that, Edit 2 is a small 2-line insert, then we move to the service file (Edit 3) and the route wiring (Edit 4).

status: str = sqlmodel.Field(default="draft", max_length=20, index=True)
    submitted_at: datetime | None = sqlmodel.Field(default=None)
    enriched: bool = sqlmodel.Field(default=False)
    enriched_json: list[dict] | None = sqlmodel.Field(default=None, sa_column=Column(JSONB))
    attributes: list["Attribute"] = Relationship(


Got it. I can see both the imports (lines 9-21) and `submit_project` (lines 276-327). Here's **Edit 4** — two find-and-replaces.

---

## Edit 4a — Add the import

**FIND** (line 11):
```
from ..db.manager import get_session
```

**REPLACE WITH:**
```
from ..db.manager import get_session
from ..services.enrichment_service import enrich_project
```

---

## Edit 4b — Add enrichment call inside `submit_project`

**FIND** (lines 316-321, the status flip and commit):
```
    project.status = "submitted"
    project.submitted_at = datetime.now(timezone.utc)
    session.add(project)
    session.commit()
    session.refresh(project)
```

**REPLACE WITH:**
```
    project.status = "submitted"
    project.submitted_at = datetime.now(timezone.utc)

    # Enrichment - runs inline, rolls back the whole submit on failure
    try:
        enriched = enrich_project(project)
        project.enriched_json = enriched
        project.enriched = True
        # Tell SQLAlchemy the JSONB column changed (in-place list mutation
        # on a JSON column is not auto-detected by the ORM).
        flag_modified(project, "enriched_json")
    except HTTPException:
        # A deliberate HTTPException from downstream should pass through.
        session.rollback()
        raise
    except Exception as e:
        session.rollback()
        log.exception("Enrichment failed for project %s", project_id)
        raise HTTPException(
            status_code=500,
            detail=f"Enrichment failed: {e}",
        )

    session.add(project)
    session.commit()
    session.refresh(project)
```

---

### Notes on what I added vs. the handoff spec

1. **`flag_modified(project, "enriched_json")`** — already imported at your line 8 (`from sqlalchemy.orm.attributes import flag_modified`). Without this, SQLAlchemy sometimes misses JSONB assignments and quietly skips the UPDATE. Since the import is already there, might as well use it defensively.

2. **Separate `HTTPException` passthrough** — if something down the call chain raises an `HTTPException` (unlikely in the current enrichment service, but future-proof), we rollback and re-raise it intact rather than wrapping it in a generic 500.

3. **`log` reference** — make sure the module has a logger defined. I didn't see the top of the file past line 21 in the screenshot, but if there's no `log = logging.getLogger(__name__)` already, you'll hit a `NameError`. Check the top of `project_routes.py` — if `log` isn't defined, add `import logging` and `log = logging.getLogger(__name__)` near the other imports before applying this edit.

Apply those and let me know when it's in. Next is Edit 5 — the `/ready-for-export` response shape.

Select-String -Path "backend\src\clarity\routes\project_routes.py" -Pattern "^log\s*=|^import logging"
